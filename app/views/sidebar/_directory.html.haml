/ .sidebar-info-box
/   .sidebar-header
/     %h3.title Recently Updated
/   .sidebar-content
/     - Group.recently_updated(5).each do |group|
/       =link_to group.name, group_path(group)
/ 
.sidebar-info-box
  .sidebar-header
    %h3.title Editing Policy
  .sidebar-content
    %p
      DGD is a community directory, so feel free to edit any page you feel you can improve. Remember, your name is permanently
      attached to any content you contribute to DGD.

.sidebar-info-box
  .sidebar-header
    %h3.title Suggested Organizations
  .sidebar-content
    %ol
    %li= link_to Group.find(rand(Group.count)).name.sub(/Dartmouth /,"").truncate(40), group_path(Group.find(rand(Group.count))), {:style => add_color(Group.find(rand(Group.count)))}
    %li= link_to Group.find(rand(Group.count)).name.sub(/Dartmouth /,"").truncate(40), group_path(Group.find(rand(Group.count))), {:style => add_color(Group.find(rand(Group.count)))}
    %li= link_to Group.find(rand(Group.count)).name.sub(/Dartmouth /,"").truncate(40), group_path(Group.find(rand(Group.count))), {:style => add_color(Group.find(rand(Group.count)))}
  

.sidebar-info-box
  .sidebar-header
    %h3.title Leaderboard
  .sidebar-content
    %ol.leaderboard
      - if @leaderboard
        - @leaderboard[0..4].each do |descriptions|
          %li= descriptions.first.user.name
    %p= link_to "More", leaderboard_group_index_path


- if @current_user.try(:admin)
  = render :partial => "sidebar/section/admin_toc"
